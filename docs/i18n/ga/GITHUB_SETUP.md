# Gaeilge (ga) — GitHub
> [Roghnóir teanga](../../LANGUAGES.md) · Is iad an bunleagan Béarla agus `LICENSE`/`NOTICE` na fréimhe atá i réim.

Dearbhaigh cearta; scan directory agus Git history do secrets; bain client/requester data, tokens, `.env`, machine files; seiceáil Apache-2.0/notices; rith `./mvnw --batch-mode --no-transfer-progress clean verify`. Cruthaigh public repo folamh `vies-client`, gan README/license nua; review staged diff; `git init -b main`, commit, athraigh `OWNER`, ansin `gh repo create OWNER/vies-client --public --source . --remote origin --push`. Cumasaigh Issues, Discussions, private vulnerability, Dependabot, secret scanning/push protection agus rules PR/approval/resolution/CI/CodeQL/dependency; cosc force push/delete. Cuir Java/VIES topics/labels. Verified donate URL i `.github/FUNDING.yml`, gan SLA. Tar éis push nuashonraigh `pom.xml` URL/SCM/developers, badges, advisory/release; signed `v1.0.0` ó green commit amháin.

