# Latviešu (lv) — GitHub
> [Valodu izvēle](../../LANGUAGES.md) · Noteicošais ir angļu tehniskais/juridiskais oriģināls un saknes `LICENSE`/`NOTICE`.

Apstiprini publicēšanas tiesības; skenē direktoriju un Git history noslēpumiem; noņem klienta/requester datus, tokenus, `.env`, mašīnas failus; pārbaudi Apache-2.0/notices; izpildi `./mvnw --batch-mode --no-transfer-progress clean verify`. Izveido tukšu public `vies-client` bez jauna README/licences, pārskati staged diff, `git init -b main`, commit, aizstāj `OWNER`, tad `gh repo create OWNER/vies-client --public --source . --remote origin --push`. Ieslēdz Issues, Discussions, private vulnerability, Dependabot, secret scanning/push protection un PR/approval/resolution/CI/CodeQL/dependency rules; aizliedz force push/delete. Pievieno Java/VIES topics/labels. Pārbaudīts donate URL `.github/FUNDING.yml` nepērk SLA. Pēc push atjauno `pom.xml` URL/SCM/developers, badges, advisory un release datus; `v1.0.0` paraksti tikai no zaļa commit.

