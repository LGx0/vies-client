# Malti (mt) — GitHub
> [Għażla tal-lingwa](../../LANGUAGES.md) · L-oriġinal Ingliż u `LICENSE`/`NOTICE` fir-root jipprevalu.

Ikkonferma drittijiet; skennja directory u Git history għal secrets; neħħi client/requester data, tokens, `.env`, machine files; ivverifika Apache-2.0/notices; agħmel `./mvnw --batch-mode --no-transfer-progress clean verify`. Oħloq public repo vojt `vies-client`, mingħajr README/license ġdid; review staged diff; `git init -b main`, commit, ibdel `OWNER`, imbagħad `gh repo create OWNER/vies-client --public --source . --remote origin --push`. Ixgħel Issues, Discussions, private vulnerability, Dependabot, secret scanning/push protection u PR/approval/resolution/CI/CodeQL/dependency rules; ipprojbixxi force push/delete. Żid Java/VIES topics/labels. Verified donate URL f’`.github/FUNDING.yml`, ebda SLA. Wara push aġġorna `pom.xml` URL/SCM/developers, badges, advisory/release; signed `v1.2.0` minn green commit biss.

